==============
FormatLabelBar
==============

``FormatLabelBar`` centers a formatted label inside a fill bar.

Reach for it to combine ``Bar`` and ``FormatLabel`` in one widget: the
label sits centered over the fill instead of beside it. ``format``
reads the same data snapshot as ``FormatLabel``.

.. autoclass:: progressbar.widgets.FormatLabelBar
   :members:
   :show-inheritance:
   :no-index:

Example
--------------------------------------------------------------------------------

.. demo:: widgets/format-label-bar

See also
--------------------------------------------------------------------------------

* :doc:`bar`: the plain fill bar this combines with a label.
* :doc:`format-label`: the label formatting this centers over the fill.
* :doc:`percentage-label-bar`: the common case, a centered percentage.
